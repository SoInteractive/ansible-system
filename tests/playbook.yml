---
- hosts: all
  pre_tasks:
    - name: Ensure sysctl.conf exists
      file:
        path: /etc/sysctl.conf
        state: touch
  roles:
    - ansible-system
  vars:
    system_upgrade: true
    system_log_aggregator: "127.0.0.1:514"
